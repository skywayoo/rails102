<div class="row">
<div class="col-md-4 col-md-offset-4">
	<h1>修改文章</h1>	
	<hr>

	<%= simple_form_for [@group,@post] do |f| %>
	<div class="form-group">
		<%= f.input :content %>
	</div>
		<%= f.submit "送出",class: "btn btn-primary",data: { disable_with: "正在送出..."} %>
		<% end %>
 </div>
 </div>